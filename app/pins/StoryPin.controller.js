/**
 * The StoryPin form controller.
 * @param $scope Angular $scope.
 * @param MapManager The MapManager.
 */
const storyPinController = ($scope, MapManager, pinSvc) => {
  const ctrl = {};
  ctrl.pins = [];
  ctrl.current_pin = null;

};