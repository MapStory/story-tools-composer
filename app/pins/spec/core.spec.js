describe("Core StoryPin functionality", () => {
  it("gets a list of Pins from the server", () => {
    let pinArray = [];
    expect(pinArray).toBe(true);
  });

  it("can create a new Pin and save it to server", () => {

  });

  it("can edit an existing Pin and save it server", () => {

  });
});
